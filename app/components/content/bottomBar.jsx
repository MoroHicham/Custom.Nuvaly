import React from 'react';

const BottomBar = () => {
    return (
      <div className="fixed bottom-0 left-0 w-full h-16 bg-gray-800 text-white flex items-center justify-center">
        {/* Content for the fixed bar */}
        This is a fixed bar at the bottom of the page.
      </div>
    );
  };
  
  export default BottomBar;